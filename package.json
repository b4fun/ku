{
  "name": "ku",
  "version": "0.0.1",
  "workspaces": [
    "browser-ui/",
    "kql/",
    "protos/"
  ],
  "scripts": {
    "build:browser-ui": "npm run build -w browser-ui",
    "build:kql": "npm run build -w kql",
    "build:protos": "npm run build -w protos",
    "build": "npm-run-all build:kql build:protos build:browser-ui",
    "test:kql": "npm run test -w kql",
    "test": "npm-run-all test:kql"
  },
  "devDependencies": {
    "@types/jest": "^28.1.6",
    "jest": "^28.1.3",
    "npm-run-all": "^4.1.5",
    "ts-jest": "^28.0.7",
    "ts-node": "^10.9.1"
  }
}